<%= form_for([link, link.comments.build], remote: true) do |f| %>
  <%= f.label :content, "Add Comment" %><br>
  <%= f.text_area :content, size: "30x5" %><br>
  <%= f.submit "Submit Comment", class: "btn btn-success btn-sm" %>
<% end %>